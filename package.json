{
    "name": "custom-diagram-boilerplate",
    "version": "0.1.0",
    "private": true,
    "devDependencies": {
        "@parcel/transformer-sass": "2.8.3",
        "parcel": "^2.8.3"
    },
    "source": "src/index.html",
    "targets": {
        "default": {
            "distDir": "./output"
        }
    },
    "browserslist": [
        "defaults",
        "last 2 years",
        "not dead"
    ],
    "scripts": {
        "start": "parcel",
        "build": "parcel build"
    }
}
